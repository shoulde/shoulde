import React from 'react';
import FirstAppContainer from './../navigator'

export default class App extends React.Component {

  
    render() {
      return (
        <FirstAppContainer />
      );
    }
}